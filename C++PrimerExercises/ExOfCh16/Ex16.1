when we call a function template,the compiler uses the arguments of the call to deduce the
template arguments for us.the compiler creates a new 'instance' of the template using the
deduced template arguments types in place of the corresponding template parameters types.
the new 'instance' is referred to as an instantiation.

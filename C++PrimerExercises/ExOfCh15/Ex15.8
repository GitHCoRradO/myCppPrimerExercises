the static type of an expression is always known at compile time.it is the type with which
a variable is declared or that an expression yields.
the dynamic type is the type of the object in memory that the variable or expression
represents.the dynamic type may not be known until run time.

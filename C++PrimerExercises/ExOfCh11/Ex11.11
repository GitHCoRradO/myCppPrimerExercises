bool (*ptr)(const Sales_data&, const Sales_data&);	//we may not initialize ptr to ponit to compareIsbn
//using ptr = bool (*)(const Sales_data&, const Sales_data&);
multiset<Sales_data, ptr> bookStore(compareIsbn);

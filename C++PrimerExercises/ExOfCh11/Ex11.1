vector is a sequential container type.elements in a vector  can only be accessed sequentially,in another word,by their positions.
map is an associative container type.elements in a map has type pair.each element contains a key and its corresponding value.we can fetch any element in a map by subscripting the map with a certain key.briefly,the elements in a map are not accessed by their "positions" but by their keys.
